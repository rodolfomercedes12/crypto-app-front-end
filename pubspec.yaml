name: crypto_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  dio: ^5.0.2
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.2
  go_router: ^6.2.0
  google_fonts: ^4.0.3
  formz: ^0.5.0+1
  flutter_dotenv: ^5.0.2
  shared_preferences: ^2.0.18
  flutter_staggered_grid_view: ^0.6.2
  image_picker: ^0.8.7
  fl_chart: ^0.62.0
  web_socket_channel: ^2.1.0
  syncfusion_flutter_charts: ^19.3.52

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/images/
    - google_fonts/montserrat_alternates/
